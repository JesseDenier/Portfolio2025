// Import necessary module from graphql-tag
import { gql } from "graphql-tag";

// Filler typeDef has been placed in until a proper schema is created.
const typeDefs = gql`
  type Query {
    hello: String
  }
`;

export default typeDefs;
